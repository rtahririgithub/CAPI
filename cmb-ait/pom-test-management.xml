<!-- generated with TAF v0.1.1 -->

<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
   xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
   <modelVersion>4.0.0</modelVersion>
   <parent>
      <groupId>telus-maven.dependencyManagement</groupId>
      <artifactId>TELUS_Base_Dependencies</artifactId>
      <version>1.3.0</version>
   </parent>

   <groupId>telus-maven.testManagement</groupId>
   <artifactId>TELUS_Integration_Test</artifactId>
   <version>1.0.0-SNAPSHOT</version>
   <packaging>pom</packaging>

   <properties>
      <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
      <serenity.version>1.0.47</serenity.version>
      <serenity.maven.version>1.0.47</serenity.maven.version>
      <serenity.browser.width>1200</serenity.browser.width>
      <serenity.browser.height>700</serenity.browser.height>
      <serenity.take.screenshots>AFTER_EACH_STEP</serenity.take.screenshots>
      <!--timeout in milliseconds-->
      <serenity.timeout>5</serenity.timeout>
      <!--timeout in seconds-->
      <webdriver.script.timeout>300</webdriver.script.timeout>
      <webdriver.driver>firefox</webdriver.driver>
      <webdriver.base.url>http://localhost:7001/AgentOfferManager</webdriver.base.url>
      <failIfNoTests>false</failIfNoTests>

      <!-- START: dynaTrace specific properties -->
      <dynaTrace.version>5.6</dynaTrace.version>
      <dynaTrace.testCategory>uidriven</dynaTrace.testCategory>
      <dynaTrace.agentGroup>JUnit</dynaTrace.agentGroup>
      <dynaTrace.profileName>DV101</dynaTrace.profileName>
      <dynaTrace.username>automation</dynaTrace.username>
      <dynaTrace.password>automation</dynaTrace.password>
      <dynaTrace.collector>http://btln000932.corp.ads:6698</dynaTrace.collector>
      <dynaTrace.serverUrl>http://btln000932.corp.ads:8020</dynaTrace.serverUrl>
      <dynaTrace.versionBuild>${BUILD_ID}</dynaTrace.versionBuild>
      <dynaTrace.versionRevision>${BUILD_NUMBER}</dynaTrace.versionRevision>
      <!-- END: dynaTrace specific properties -->
      <junit.version>4.11</junit.version>
   </properties>

   <dependencyManagement>
      <dependencies>
         <dependency>
           <groupId>net.serenity-bdd</groupId>
           <artifactId>core</artifactId>
           <version>${serenity.version}</version>
         </dependency>
         <dependency>
            <groupId>junit</groupId>
            <artifactId>junit</artifactId>
            <version>${junit.version}</version>
         </dependency>
          <dependency>
            <groupId>net.serenity-bdd</groupId>
            <artifactId>serenity-junit</artifactId>
            <version>${serenity.version}</version>
          </dependency>
          <dependency>
            <groupId>net.serenity-bdd</groupId>
            <artifactId>serenity-spring</artifactId>
            <version>${serenity.version}</version>
          </dependency>
      </dependencies>
   </dependencyManagement>

   <dependencies>
      <dependency>
         <groupId>junit</groupId>
         <artifactId>junit</artifactId>
      </dependency>
   </dependencies>

   <build>
      <plugins>
         <plugin>
            <groupId>org.apache.maven.plugins</groupId>
            <artifactId>maven-surefire-plugin</artifactId>
            <version>2.12</version>
            <configuration>
               <skip>true</skip>
            </configuration>
         </plugin>
         <plugin>
            <artifactId>maven-failsafe-plugin</artifactId>
            <version>2.17</version>
            <configuration>
               <includes>
                  <include>**/*Test.java</include>
                  <include>**/Test*.java</include>
                  <include>**/When*.java</include>
               </includes>
               <argLine>-Xmx512m</argLine>
               <systemProperties>
                  <property>
                     <name>com.dynatrace.diagnostics.ci.testrunID</name>
                     <value>${dtTestrunID}</value>
                  </property>
               </systemProperties>
               <systemPropertyVariables>
                  <webdriver.driver>${webdriver.driver}</webdriver.driver>
                  <webdriver.base.url>${webdriver.base.url}</webdriver.base.url>
                  <serenity.browser.width>${serenity.browser.width}</serenity.browser.width>
                  <serenity.browser.height>${serenity.browser.height}</serenity.browser.height>
                  <serenity.take.screenshots>${serenity.take.screenshots}</serenity.take.screenshots>
                  <serenity.timeout>${serenity.timeout}</serenity.timeout>
                  <webdriver.script.timeout>${webdriver.script.timeout}</webdriver.script.timeout>
               </systemPropertyVariables>
            </configuration>
            <executions>
               <execution>
                  <goals>
                     <goal>integration-test</goal>
                     <goal>verify</goal>
                  </goals>
               </execution>
            </executions>
         </plugin>
         <plugin>
            <groupId>dynaTrace</groupId>
            <artifactId>dtAutomation</artifactId>
            <version>${dynaTrace.version}</version>
            <configuration>
               <category>${dynaTrace.testCategory}</category>
               <versionRevision>${dynaTrace.versionRevision}</versionRevision>
               <versionBuild>${dynaTrace.versionBuild}</versionBuild>

               <profileName>${dynaTrace.profileName}</profileName>
               <username>${dynaTrace.username}</username>
               <password>${dynaTrace.password}</password>
               <serverUrl>${dynaTrace.serverUrl}</serverUrl>
               <additionalProperties></additionalProperties>
            </configuration>
         </plugin>
      </plugins>
      <pluginManagement>
         <plugins>
            <plugin>
               <groupId>net.serenity-bdd.maven.plugins</groupId>
               <artifactId>serenity-maven-plugin</artifactId>
               <version>${serenity.maven.version}</version>
               <executions>
                  <execution>
                     <id>serenity-reports</id>
                     <phase>post-integration-test</phase>
                     <goals>
                        <goal>aggregate</goal>
                     </goals>
                  </execution>
               </executions>
               <dependencies>
                  <dependency>
                    <groupId>net.serenity-bdd</groupId>
                    <artifactId>core</artifactId>
                    <version>${serenity.version}</version>
                  </dependency>
               </dependencies>
            </plugin>
         </plugins>
      </pluginManagement>
   </build>

   <profiles>
      <profile>
         <id>BuildServer</id>
         <activation>
            <file>
               <exists>${basedir}</exists>
            </file>
         </activation>
         <build>
            <plugins>
               <plugin>
                  <groupId>org.apache.maven.plugins</groupId>
                  <artifactId>maven-site-plugin</artifactId>
                  <version>3.0-beta-3</version>
                  <configuration>
                     <reportPlugins>
                        <plugin>
                          <groupId>net.serenity-bdd.maven.plugins</groupId>
                          <artifactId>serenity-maven-plugin</artifactId>
                          <version>${serenity.maven.version}</version>
                        </plugin>
                     </reportPlugins>
                  </configuration>
               </plugin>
            </plugins>
         </build>
      </profile>
   </profiles>

</project>
